// lib/UI/card2.dart
import 'package:flutter/material.dart'; // DOIT être la première ligne non-commentaire

class Card2 extends StatelessWidget {
  const Card2({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Écran Card 2', style: TextStyle(fontSize: 22)));
  }
}